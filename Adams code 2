<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>MDE Premium</title>

<link rel="manifest" href="manifest.json">

<style>

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:'Segoe UI',sans-serif;
}

body{
background:linear-gradient(135deg,#4facfe,#00f2fe);
min-height:100vh;
display:flex;
justify-content:center;
align-items:center;
}

.container{
width:420px;
max-width:95%;
background:white;
border-radius:25px;
box-shadow:0 20px 60px rgba(0,0,0,0.2);
overflow:hidden;
}

.header{
padding:25px;
text-align:center;
background:#4CAF50;
color:white;
font-size:22px;
font-weight:bold;
}

.content{
padding:25px;
}

.rate{
font-size:26px;
font-weight:bold;
text-align:center;
margin-bottom:20px;
}

input{
width:100%;
padding:15px;
border-radius:12px;
border:none;
background:#f1f3f6;
margin-bottom:15px;
font-size:16px;
}

button{
width:100%;
padding:15px;
border:none;
border-radius:12px;
background:#4CAF50;
color:white;
font-weight:bold;
font-size:16px;
cursor:pointer;
}

.result{
margin-top:20px;
padding:20px;
border-radius:15px;
background:#E8F5E9;
font-size:24px;
font-weight:bold;
text-align:center;
display:none;
}

.share{
position:fixed;
bottom:25px;
right:25px;
width:65px;
height:65px;
border-radius:50%;
background:#4CAF50;
color:white;
display:flex;
justify-content:center;
align-items:center;
font-size:28px;
cursor:pointer;
box-shadow:0 10px 30px rgba(0,0,0,.3);
}

.install{
margin-top:10px;
background:#2196F3;
}

</style>
</head>

<body>

<div class="container">

<div class="header">
🌍 MDE PREMIUM
</div>

<div class="content">

<div class="rate" id="rate"></div>

<input type="number" id="amount" placeholder="Enter Rands (ZAR)">

<button onclick="convert()">Convert to MWK</button>

<button class="install" id="installBtn" style="display:none;">
Install App 📲
</button>

<div class="result" id="result"></div>

</div>
</div>

<div class="share" onclick="shareApp()">📤</div>

<script>

/* RATE */
let rate = localStorage.getItem('mde_rate') || 18.50;

document.getElementById('rate').innerText =
"1 ZAR = "+rate+" MWK";

/* CONVERT */
function convert(){

let amount=document.getElementById('amount').value;

if(!amount) return alert("Enter amount");

let result=amount*rate;

document.getElementById('result').style.display='block';
document.getElementById('result').innerHTML=
amount+" ZAR = "+result.toLocaleString()+" MWK";
}

/* SHARE */
function shareApp(){

let text="🌍 Download MDE App — Best ZAR to MWK Converter! "+location.href;

if(navigator.share){
navigator.share({
title:"MDE App",
text:text,
url:location.href
});
}else{
navigator.clipboard.writeText(text);
alert("Link copied!");
}
}

/* INSTALL BUTTON */
let deferredPrompt;

window.addEventListener('beforeinstallprompt',(e)=>{
e.preventDefault();
deferredPrompt=e;
installBtn.style.display='block';
});

installBtn.onclick=()=>{
deferredPrompt.prompt();
};

/* SERVICE WORKER */
if('serviceWorker' in navigator){
navigator.serviceWorker.register('sw.js');
}

</script>

</body>
</html>
